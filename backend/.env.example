# MongoDB connection string (replace <host> with your Mongo host or service name in Docker)
MONGO_URL=mongodb://<host>:27017

# Database name
MONGO_DB=nftstore

# Optional: backend port (defaults to 3000 if not set)
PORT=3000
